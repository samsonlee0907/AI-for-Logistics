# The portal is mock-first. Set WEBIQ_ENABLED=true only with both values below.
WEBIQ_BASE_URL=https://api.microsoft.ai/v3
WEBIQ_API_KEY=
WEBIQ_ENABLED=false

# Optional Azure OpenAI / Microsoft Foundry advisory provider. It never calculates operations.
FOUNDRY_ENABLED=false
FOUNDRY_ENDPOINT=
FOUNDRY_API_KEY=
FOUNDRY_DEPLOYMENT=
PORT=3000
